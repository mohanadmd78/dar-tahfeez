# دليل تشغيل نظام دار التحفيظ (Supabase + Next.js)

هذا يلغي نسخة Google Sheets السابقة بالكامل — نظام جديد من الصفر [حسب طلبك].

---

## الخطوة 1: إنشاء مشروع Supabase (مجاني)

1. افتح [supabase.com](https://supabase.com) وسجّل حساب (بريد أو GitHub).
2. **New project** → اختر اسمًا (مثلاً `dar-tahfeez`) وكلمة مرور لقاعدة البيانات (احفظها بمكان آمن، لن تحتاجها يوميًا لكنك قد تحتاجها لاحقًا).
3. اختر المنطقة الأقرب لك جغرافيًا (مثلاً Europe/Frankfurt أفضل من أمريكا بالنسبة لك من لبنان).
4. انتظر بضع دقائق حتى يجهّز Supabase المشروع.

## الخطوة 2: تشغيل مخطط قاعدة البيانات

1. من القائمة الجانبية بمشروع Supabase: **SQL Editor** → **New query**.
2. افتح ملف `schema.sql` المرفق، انسخ محتواه كاملاً، الصقه، واضغط **Run**.
3. تأكد من عدم وجود أخطاء حمراء. اذهب لـ **Table Editor** وتحقق من وجود جداول: `students`، `daily_logs`، `days_off`.

## الخطوة 3: إنشاء حساب المسؤول (أنت فقط)

1. من القائمة الجانبية: **Authentication** → **Users** → **Add user** → **Create new user**.
2. أدخل بريدك الإلكتروني وكلمة مرور. فعّل **Auto Confirm User** لتفادي إيميل التأكيد.
3. **مهم**: من **Authentication** → **Providers** → تأكد أن **Email** مفعّل، ومن **Authentication** → **Settings** عطّل **Allow new users to sign up** (Disable signups) — بهذا لا يقدر أي شخص آخر ينشئ حساب لنفسه حتى لو حصل على الرابط.

## الخطوة 4: الحصول على مفاتيح الربط

1. من **Project Settings** (⚙) → **API**.
2. انسخ:
   - **Project URL**
   - **anon public key**
3. بمجلد المشروع، انسخ ملف `.env.local.example` وسمِّه `.env.local`، وضع القيمتين:
   ```
   NEXT_PUBLIC_SUPABASE_URL=...
   NEXT_PUBLIC_SUPABASE_ANON_KEY=...
   ```

## الخطوة 5: التشغيل محليًا للتجربة

بما إنك مرتاح بـ Next.js من قبل، هاي الخطوات معروفة إلك:

```bash
npm install
npm run dev
```

افتح `http://localhost:3000`، سجّل دخول بالحساب يلي أنشأته بالخطوة 3.

## الخطوة 6: النشر (Vercel، مجاني)

1. ادفع المشروع لمستودع GitHub خاص (Private repo — مهم يكون خاص).
2. افتح [vercel.com](https://vercel.com) → **Add New Project** → اربط المستودع.
3. بقسم **Environment Variables** ضيف نفس المتغيرين من الخطوة 4.
4. **Deploy**. بعد الانتهاء بتاخد رابط مباشر (مثال: `dar-tahfeez.vercel.app`).

## ليش هذا النظام أأمن من نسخة Sheets

- الحماية هون فعلية على مستوى قاعدة البيانات (RLS)، مش بس واجهة أمامية — حتى لو حدا فتح أدوات المطوّر ما بيقدر يوصل لأي بيانات بدون تسجيل دخول حقيقي عبر Supabase Auth.
- تعطيل التسجيل العام (الخطوة 3) يعني ما في طريقة لأي شخص ينشئ حساب لنفسه حتى لو عنده الرابط.

## حدود يجب أن تعرفها [مرجح]

- **الخطة المجانية بـ Supabase**: المشروع يُوقَّف (pause) تلقائيًا بعد أسبوع من عدم الاستخدام على الخطة المجانية — إذا ما فتحت النظام لمدة أطول من أسبوع، أول دخول بعدها ممكن ياخد دقيقة لإعادة التشغيل، هذا طبيعي.
- **500 ميجابايت تخزين** كافية جدًا لعدد طلاب أقل من 30 لسنوات طويلة، لكن إذا رفعت صور أو ملفات لاحقًا فهذا يستهلك المساحة أسرع من النصوص.
- **نسخ احتياطي**: الخطة المجانية لا توفر نسخًا احتياطية تلقائية طويلة المدى. يُفضّل تصدير البيانات يدويًا كل فترة (Table Editor → تصدير CSV) أو الترقية لخطة مدفوعة إذا أصبحت البيانات حساسة جدًا.
